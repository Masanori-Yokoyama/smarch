[global]
   workgroup = WORKGROUP
   server string = Samba Server
   security = user
   map to guest = bad user
   dns proxy = no

[share]
   path = /share
   browseable = yes
   writable = yes
   guest ok = yes
   read only = no
